services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: daily_snap
      POSTGRES_PASSWORD: daily_snap_dev
      POSTGRES_DB: daily_snap
      TZ: Asia/Tokyo
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
